<!---
	This is the parent controller file that all your controllers should extend.
	You can add functions to this file to make them globally available in all your controllers.
	Do not delete this file.
--->
<cfcomponent extends="Wheels">
	
	<cffunction name="spokeCheckLogin" output="false" returnType="boolean" access="private" hint="
		This is where you would put session timeout/logout logic, for whether the view uses an inline popup or redirects see the views.
		Should return true if logged in, false otherwise.
		If do not need to check for sessions just return true.">
		<cfscript>
			return true;
		</cfscript>
	</cffunction>
	
</cfcomponent>